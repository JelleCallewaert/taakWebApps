import { DoelgroepFilterPipe } from './doelgroep-filter.pipe';

describe('DoelgroepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoelgroepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
