import { UsedDoelgroepFilterPipe } from './used-doelgroep-filter.pipe';

describe('UsedDoelgroepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsedDoelgroepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
